<UserControl
    x:Class="AmbientSounds.Controls.AssetRowControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:animatedvisuals="using:AnimatedVisuals"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ex="using:JeniusApps.Common.UI.Uwp"
    xmlns:labs="using:CommunityToolkit.Labs.WinUI"
    xmlns:local="using:AmbientSounds.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:winui="using:Microsoft.UI.Xaml.Controls"
    d:DesignHeight="300"
    d:DesignWidth="400"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock
                VerticalAlignment="Center"
                FontSize="14"
                FontWeight="SemiBold"
                Text="{x:Bind TitleText, Mode=OneWay}"
                TextTrimming="CharacterEllipsis" />

            <winui:AnimatedVisualPlayer
                x:Name="SparkleIcon"
                Grid.Column="1"
                Height="20"
                HorizontalAlignment="Left"
                x:Load="{x:Bind SparkleIconVisible, Mode=OneWay}"
                AutoPlay="True">
                <animatedvisuals:SparkleLottie />
            </winui:AnimatedVisualPlayer>
        </Grid>

        <ItemsControl
            Grid.Row="1"
            Margin="0,8,16,0"
            ItemTemplate="{x:Bind ItemTemplate, Mode=OneWay}"
            ItemsPanel="{x:Bind ItemsPanel, Mode=OneWay}"
            ItemsSource="{x:Bind ItemsSource, Mode=OneWay}" />

        <labs:Shimmer
            Grid.Row="1"
            Height="{x:Bind ShimmerHeight, Mode=OneWay}"
            Margin="0,8,16,0"
            HorizontalAlignment="Stretch"
            AutomationProperties.AccessibilityView="Raw"
            Visibility="{x:Bind ex:UIExtensions.InvertBoolToVis(ItemsVisible), Mode=OneWay}" />
    </Grid>
</UserControl>
